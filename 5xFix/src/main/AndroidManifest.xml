<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dr8.xposed.allyfix5x">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ally5xfix"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data android:value="true" android:name="xposedmodule"/>
        <meta-data android:value="2.0*" android:name="xposedminversion"/>
        <meta-data android:value="Fixes camera rotation for Ally Mobile deposits on Nexus 5x" android:name="xposeddescription"/>
    </application>

</manifest>
